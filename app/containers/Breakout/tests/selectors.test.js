// import { fromJS } from 'immutable';
// import { makeSelectBreakoutDomain } from '../selectors';

// const selector = makeSelectBreakoutDomain();

describe('makeSelectBreakoutDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
